//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::AddKeyInput;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;

/// Accounts.
#[derive(Debug)]
pub struct AdjustAuth {
    /// The funder for the profile.
    pub funder: solana_program::pubkey::Pubkey,
    /// The profile to create
    pub profile: solana_program::pubkey::Pubkey,
    /// The system program
    pub system_program: solana_program::pubkey::Pubkey,
}

impl AdjustAuth {
    pub fn instruction(
        &self,
        args: AdjustAuthInstructionArgs,
    ) -> solana_program::instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: AdjustAuthInstructionArgs,
        remaining_accounts: &[solana_program::instruction::AccountMeta],
    ) -> solana_program::instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.funder,
            true,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.profile,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = borsh::to_vec(&AdjustAuthInstructionData::new()).unwrap();
        let mut args = borsh::to_vec(&args).unwrap();
        data.append(&mut args);

        solana_program::instruction::Instruction {
            program_id: crate::PLAYER_PROFILE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct AdjustAuthInstructionData {
    discriminator: [u8; 8],
}

impl AdjustAuthInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [6, 254, 177, 87, 111, 72, 12, 211],
        }
    }
}

impl Default for AdjustAuthInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct AdjustAuthInstructionArgs {
    pub auth_indexes: Vec<u16>,
    pub new_key_permissions: Vec<AddKeyInput>,
    pub remove_range: [u16; 2],
    pub new_key_threshold: u8,
}

/// Instruction builder for `AdjustAuth`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` funder
///   1. `[writable]` profile
///   2. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct AdjustAuthBuilder {
    funder: Option<solana_program::pubkey::Pubkey>,
    profile: Option<solana_program::pubkey::Pubkey>,
    system_program: Option<solana_program::pubkey::Pubkey>,
    auth_indexes: Option<Vec<u16>>,
    new_key_permissions: Option<Vec<AddKeyInput>>,
    remove_range: Option<[u16; 2]>,
    new_key_threshold: Option<u8>,
    __remaining_accounts: Vec<solana_program::instruction::AccountMeta>,
}

impl AdjustAuthBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The funder for the profile.
    #[inline(always)]
    pub fn funder(&mut self, funder: solana_program::pubkey::Pubkey) -> &mut Self {
        self.funder = Some(funder);
        self
    }
    /// The profile to create
    #[inline(always)]
    pub fn profile(&mut self, profile: solana_program::pubkey::Pubkey) -> &mut Self {
        self.profile = Some(profile);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    /// The system program
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_program::pubkey::Pubkey) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn auth_indexes(&mut self, auth_indexes: Vec<u16>) -> &mut Self {
        self.auth_indexes = Some(auth_indexes);
        self
    }
    #[inline(always)]
    pub fn new_key_permissions(&mut self, new_key_permissions: Vec<AddKeyInput>) -> &mut Self {
        self.new_key_permissions = Some(new_key_permissions);
        self
    }
    #[inline(always)]
    pub fn remove_range(&mut self, remove_range: [u16; 2]) -> &mut Self {
        self.remove_range = Some(remove_range);
        self
    }
    #[inline(always)]
    pub fn new_key_threshold(&mut self, new_key_threshold: u8) -> &mut Self {
        self.new_key_threshold = Some(new_key_threshold);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: solana_program::instruction::AccountMeta,
    ) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_program::instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_program::instruction::Instruction {
        let accounts = AdjustAuth {
            funder: self.funder.expect("funder is not set"),
            profile: self.profile.expect("profile is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_program::pubkey!("11111111111111111111111111111111")),
        };
        let args = AdjustAuthInstructionArgs {
            auth_indexes: self.auth_indexes.clone().expect("auth_indexes is not set"),
            new_key_permissions: self
                .new_key_permissions
                .clone()
                .expect("new_key_permissions is not set"),
            remove_range: self.remove_range.clone().expect("remove_range is not set"),
            new_key_threshold: self
                .new_key_threshold
                .clone()
                .expect("new_key_threshold is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `adjust_auth` CPI accounts.
pub struct AdjustAuthCpiAccounts<'a, 'b> {
    /// The funder for the profile.
    pub funder: &'b solana_program::account_info::AccountInfo<'a>,
    /// The profile to create
    pub profile: &'b solana_program::account_info::AccountInfo<'a>,
    /// The system program
    pub system_program: &'b solana_program::account_info::AccountInfo<'a>,
}

/// `adjust_auth` CPI instruction.
pub struct AdjustAuthCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_program::account_info::AccountInfo<'a>,
    /// The funder for the profile.
    pub funder: &'b solana_program::account_info::AccountInfo<'a>,
    /// The profile to create
    pub profile: &'b solana_program::account_info::AccountInfo<'a>,
    /// The system program
    pub system_program: &'b solana_program::account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: AdjustAuthInstructionArgs,
}

impl<'a, 'b> AdjustAuthCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_program::account_info::AccountInfo<'a>,
        accounts: AdjustAuthCpiAccounts<'a, 'b>,
        args: AdjustAuthInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            funder: accounts.funder,
            profile: accounts.profile,
            system_program: accounts.system_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.funder.key,
            true,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.profile.key,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_program::instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = borsh::to_vec(&AdjustAuthInstructionData::new()).unwrap();
        let mut args = borsh::to_vec(&self.__args).unwrap();
        data.append(&mut args);

        let instruction = solana_program::instruction::Instruction {
            program_id: crate::PLAYER_PROFILE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.funder.clone());
        account_infos.push(self.profile.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_program::program::invoke(&instruction, &account_infos)
        } else {
            solana_program::program::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `AdjustAuth` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` funder
///   1. `[writable]` profile
///   2. `[]` system_program
#[derive(Clone, Debug)]
pub struct AdjustAuthCpiBuilder<'a, 'b> {
    instruction: Box<AdjustAuthCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> AdjustAuthCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_program::account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(AdjustAuthCpiBuilderInstruction {
            __program: program,
            funder: None,
            profile: None,
            system_program: None,
            auth_indexes: None,
            new_key_permissions: None,
            remove_range: None,
            new_key_threshold: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The funder for the profile.
    #[inline(always)]
    pub fn funder(
        &mut self,
        funder: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.funder = Some(funder);
        self
    }
    /// The profile to create
    #[inline(always)]
    pub fn profile(
        &mut self,
        profile: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.profile = Some(profile);
        self
    }
    /// The system program
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn auth_indexes(&mut self, auth_indexes: Vec<u16>) -> &mut Self {
        self.instruction.auth_indexes = Some(auth_indexes);
        self
    }
    #[inline(always)]
    pub fn new_key_permissions(&mut self, new_key_permissions: Vec<AddKeyInput>) -> &mut Self {
        self.instruction.new_key_permissions = Some(new_key_permissions);
        self
    }
    #[inline(always)]
    pub fn remove_range(&mut self, remove_range: [u16; 2]) -> &mut Self {
        self.instruction.remove_range = Some(remove_range);
        self
    }
    #[inline(always)]
    pub fn new_key_threshold(&mut self, new_key_threshold: u8) -> &mut Self {
        self.instruction.new_key_threshold = Some(new_key_threshold);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_program::account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        let args = AdjustAuthInstructionArgs {
            auth_indexes: self
                .instruction
                .auth_indexes
                .clone()
                .expect("auth_indexes is not set"),
            new_key_permissions: self
                .instruction
                .new_key_permissions
                .clone()
                .expect("new_key_permissions is not set"),
            remove_range: self
                .instruction
                .remove_range
                .clone()
                .expect("remove_range is not set"),
            new_key_threshold: self
                .instruction
                .new_key_threshold
                .clone()
                .expect("new_key_threshold is not set"),
        };
        let instruction = AdjustAuthCpi {
            __program: self.instruction.__program,

            funder: self.instruction.funder.expect("funder is not set"),

            profile: self.instruction.profile.expect("profile is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct AdjustAuthCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_program::account_info::AccountInfo<'a>,
    funder: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    profile: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    auth_indexes: Option<Vec<u16>>,
    new_key_permissions: Option<Vec<AddKeyInput>>,
    remove_range: Option<[u16; 2]>,
    new_key_threshold: Option<u8>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(
        &'b solana_program::account_info::AccountInfo<'a>,
        bool,
        bool,
    )>,
}
