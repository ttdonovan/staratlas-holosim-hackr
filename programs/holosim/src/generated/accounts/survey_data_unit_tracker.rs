//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_pubkey::Pubkey;

/// Survey Data Unit (SDU) Tracker

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct SurveyDataUnitTracker {
    pub discriminator: [u8; 8],
    /// The data version of this account.
    pub version: u8,
    /// The game_id that this belongs to
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub game_id: Pubkey,
    /// The Survey Data Unit Mint
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub sdu_mint: Pubkey,
    /// The mint of the resource spent when scanning for SDUs
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub resource_mint: Pubkey,
    /// The signer for this account
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub signer: Pubkey,
    /// The signer for this account
    pub signer_bump: u8,
    /// The valid coordinates range
    /// e.g. a value of [-50, 50] means that coordinates from [-50, -50] to [50, 50] are valid for SDU scanning
    pub coordinates_range: [i64; 2],
    /// The locations of the central space stations (CSS) of the three factions
    pub css_coordinates: [[i64; 2]; 3],
    /// The co-ordinates of the "origin"; used in calculating SDU probability
    pub origin_coordinates: [i64; 2],
    /// The max distance from the nearest CSS; used in calculating SDU probability
    pub css_max_distance: u32,
    /// The max distance from the `origin_coordinates`; used in calculating SDU probability
    pub origin_max_distance: u32,
    /// The distance weighting; used in calculating SDU probability
    pub distance_weighting: u32,
    /// The maximum time before SDU probability at a location changes
    pub t_max: i64,
    /// Multiplier in the X dimension; used in noise function
    pub x_mul: u32,
    /// Multiplier in the Y dimension; used in noise function
    pub y_mul: u32,
    /// Multiplier in the Z dimension; used in noise function
    pub z_mul: u32,
    /// The maximum number of SDUs that can be found per scan per sector
    pub sdu_max_per_sector: u32,
    /// The amount of time in seconds that it takes for a sector scan chance to fully regenerate
    pub scan_chance_regen_period: i16,
}

pub const SURVEY_DATA_UNIT_TRACKER_DISCRIMINATOR: [u8; 8] = [234, 127, 227, 90, 144, 65, 85, 111];

impl SurveyDataUnitTracker {
    pub const LEN: usize = 256;

    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for SurveyDataUnitTracker {
    type Error = std::io::Error;

    fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
        let mut data: &[u8] = &(*account_info.data).borrow();
        Self::deserialize(&mut data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_survey_data_unit_tracker(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<SurveyDataUnitTracker>, std::io::Error> {
    let accounts = fetch_all_survey_data_unit_tracker(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_survey_data_unit_tracker(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<SurveyDataUnitTracker>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<SurveyDataUnitTracker>> =
        Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::new(
            std::io::ErrorKind::Other,
            format!("Account not found: {}", address),
        ))?;
        let data = SurveyDataUnitTracker::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount {
            address,
            account: account.clone(),
            data,
        });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_survey_data_unit_tracker(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<SurveyDataUnitTracker>, std::io::Error> {
    let accounts = fetch_all_maybe_survey_data_unit_tracker(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_survey_data_unit_tracker(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<SurveyDataUnitTracker>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<SurveyDataUnitTracker>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            let data = SurveyDataUnitTracker::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(
                crate::shared::DecodedAccount {
                    address,
                    account: account.clone(),
                    data,
                },
            ));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountDeserialize for SurveyDataUnitTracker {
    fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
    }
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountSerialize for SurveyDataUnitTracker {}

#[cfg(feature = "anchor")]
impl anchor_lang::Owner for SurveyDataUnitTracker {
    fn owner() -> Pubkey {
        crate::SAGE_ID
    }
}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::IdlBuild for SurveyDataUnitTracker {}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::Discriminator for SurveyDataUnitTracker {
    const DISCRIMINATOR: &[u8] = &[0; 8];
}
