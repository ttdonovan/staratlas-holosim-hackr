//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

/// Accounts.
#[derive(Debug)]
pub struct InvalidateRental {
    /// The fleet rental invalidator
    pub sub_profile_invalidator: solana_program::pubkey::Pubkey,
    /// The fleet
    pub fleet: solana_program::pubkey::Pubkey,
}

impl InvalidateRental {
    pub fn instruction(
        &self,
        args: InvalidateRentalInstructionArgs,
    ) -> solana_program::instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: InvalidateRentalInstructionArgs,
        remaining_accounts: &[solana_program::instruction::AccountMeta],
    ) -> solana_program::instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            self.sub_profile_invalidator,
            true,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.fleet, false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = borsh::to_vec(&InvalidateRentalInstructionData::new()).unwrap();
        let mut args = borsh::to_vec(&args).unwrap();
        data.append(&mut args);

        solana_program::instruction::Instruction {
            program_id: crate::SAGE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct InvalidateRentalInstructionData {
    discriminator: [u8; 8],
}

impl InvalidateRentalInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [180, 183, 106, 153, 255, 8, 232, 178],
        }
    }
}

impl Default for InvalidateRentalInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct InvalidateRentalInstructionArgs {
    pub remove_invalidator: bool,
}

/// Instruction builder for `InvalidateRental`.
///
/// ### Accounts:
///
///   0. `[signer]` sub_profile_invalidator
///   1. `[writable]` fleet
#[derive(Clone, Debug, Default)]
pub struct InvalidateRentalBuilder {
    sub_profile_invalidator: Option<solana_program::pubkey::Pubkey>,
    fleet: Option<solana_program::pubkey::Pubkey>,
    remove_invalidator: Option<bool>,
    __remaining_accounts: Vec<solana_program::instruction::AccountMeta>,
}

impl InvalidateRentalBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The fleet rental invalidator
    #[inline(always)]
    pub fn sub_profile_invalidator(
        &mut self,
        sub_profile_invalidator: solana_program::pubkey::Pubkey,
    ) -> &mut Self {
        self.sub_profile_invalidator = Some(sub_profile_invalidator);
        self
    }
    /// The fleet
    #[inline(always)]
    pub fn fleet(&mut self, fleet: solana_program::pubkey::Pubkey) -> &mut Self {
        self.fleet = Some(fleet);
        self
    }
    #[inline(always)]
    pub fn remove_invalidator(&mut self, remove_invalidator: bool) -> &mut Self {
        self.remove_invalidator = Some(remove_invalidator);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: solana_program::instruction::AccountMeta,
    ) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_program::instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_program::instruction::Instruction {
        let accounts = InvalidateRental {
            sub_profile_invalidator: self
                .sub_profile_invalidator
                .expect("sub_profile_invalidator is not set"),
            fleet: self.fleet.expect("fleet is not set"),
        };
        let args = InvalidateRentalInstructionArgs {
            remove_invalidator: self
                .remove_invalidator
                .clone()
                .expect("remove_invalidator is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `invalidate_rental` CPI accounts.
pub struct InvalidateRentalCpiAccounts<'a, 'b> {
    /// The fleet rental invalidator
    pub sub_profile_invalidator: &'b solana_program::account_info::AccountInfo<'a>,
    /// The fleet
    pub fleet: &'b solana_program::account_info::AccountInfo<'a>,
}

/// `invalidate_rental` CPI instruction.
pub struct InvalidateRentalCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_program::account_info::AccountInfo<'a>,
    /// The fleet rental invalidator
    pub sub_profile_invalidator: &'b solana_program::account_info::AccountInfo<'a>,
    /// The fleet
    pub fleet: &'b solana_program::account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: InvalidateRentalInstructionArgs,
}

impl<'a, 'b> InvalidateRentalCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_program::account_info::AccountInfo<'a>,
        accounts: InvalidateRentalCpiAccounts<'a, 'b>,
        args: InvalidateRentalInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            sub_profile_invalidator: accounts.sub_profile_invalidator,
            fleet: accounts.fleet,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new_readonly(
            *self.sub_profile_invalidator.key,
            true,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.fleet.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_program::instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = borsh::to_vec(&InvalidateRentalInstructionData::new()).unwrap();
        let mut args = borsh::to_vec(&self.__args).unwrap();
        data.append(&mut args);

        let instruction = solana_program::instruction::Instruction {
            program_id: crate::SAGE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.sub_profile_invalidator.clone());
        account_infos.push(self.fleet.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_program::program::invoke(&instruction, &account_infos)
        } else {
            solana_program::program::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `InvalidateRental` via CPI.
///
/// ### Accounts:
///
///   0. `[signer]` sub_profile_invalidator
///   1. `[writable]` fleet
#[derive(Clone, Debug)]
pub struct InvalidateRentalCpiBuilder<'a, 'b> {
    instruction: Box<InvalidateRentalCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> InvalidateRentalCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_program::account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(InvalidateRentalCpiBuilderInstruction {
            __program: program,
            sub_profile_invalidator: None,
            fleet: None,
            remove_invalidator: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The fleet rental invalidator
    #[inline(always)]
    pub fn sub_profile_invalidator(
        &mut self,
        sub_profile_invalidator: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.sub_profile_invalidator = Some(sub_profile_invalidator);
        self
    }
    /// The fleet
    #[inline(always)]
    pub fn fleet(&mut self, fleet: &'b solana_program::account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.fleet = Some(fleet);
        self
    }
    #[inline(always)]
    pub fn remove_invalidator(&mut self, remove_invalidator: bool) -> &mut Self {
        self.instruction.remove_invalidator = Some(remove_invalidator);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_program::account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        let args = InvalidateRentalInstructionArgs {
            remove_invalidator: self
                .instruction
                .remove_invalidator
                .clone()
                .expect("remove_invalidator is not set"),
        };
        let instruction = InvalidateRentalCpi {
            __program: self.instruction.__program,

            sub_profile_invalidator: self
                .instruction
                .sub_profile_invalidator
                .expect("sub_profile_invalidator is not set"),

            fleet: self.instruction.fleet.expect("fleet is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct InvalidateRentalCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_program::account_info::AccountInfo<'a>,
    sub_profile_invalidator: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    fleet: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    remove_invalidator: Option<bool>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(
        &'b solana_program::account_info::AccountInfo<'a>,
        bool,
        bool,
    )>,
}
