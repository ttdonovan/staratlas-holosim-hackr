//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const UPDATE_SHIP_IN_FLEET_DISCRIMINATOR: [u8; 8] = [213, 83, 186, 229, 179, 31, 172, 253];

/// Accounts.
#[derive(Debug)]
pub struct UpdateShipInFleet {
    /// The [`Fleet`] account
    pub fleet: solana_pubkey::Pubkey,
    /// The [`FleetShips`] account
    pub fleet_ships: solana_pubkey::Pubkey,
    /// The old [`Ship`] Account
    pub old_ship: solana_pubkey::Pubkey,
    /// The address indicated as `next` in the `old_ship` account
    pub next: solana_pubkey::Pubkey,
    /// The [`Game`] account
    pub game_id: solana_pubkey::Pubkey,
    /// The [`GameState`] account
    pub game_state: solana_pubkey::Pubkey,
}

impl UpdateShipInFleet {
    pub fn instruction(
        &self,
        args: UpdateShipInFleetInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: UpdateShipInFleetInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(6 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.fleet, false));
        accounts.push(solana_instruction::AccountMeta::new(
            self.fleet_ships,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.old_ship,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.next, false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.game_id,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.game_state,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = borsh::to_vec(&UpdateShipInFleetInstructionData::new()).unwrap();
        let mut args = borsh::to_vec(&args).unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::SAGE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateShipInFleetInstructionData {
    discriminator: [u8; 8],
}

impl UpdateShipInFleetInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [213, 83, 186, 229, 179, 31, 172, 253],
        }
    }
}

impl Default for UpdateShipInFleetInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateShipInFleetInstructionArgs {
    pub ship_amount: u16,
    pub fleet_ship_info_index: u32,
}

/// Instruction builder for `UpdateShipInFleet`.
///
/// ### Accounts:
///
///   0. `[writable]` fleet
///   1. `[writable]` fleet_ships
///   2. `[]` old_ship
///   3. `[]` next
///   4. `[]` game_id
///   5. `[]` game_state
#[derive(Clone, Debug, Default)]
pub struct UpdateShipInFleetBuilder {
    fleet: Option<solana_pubkey::Pubkey>,
    fleet_ships: Option<solana_pubkey::Pubkey>,
    old_ship: Option<solana_pubkey::Pubkey>,
    next: Option<solana_pubkey::Pubkey>,
    game_id: Option<solana_pubkey::Pubkey>,
    game_state: Option<solana_pubkey::Pubkey>,
    ship_amount: Option<u16>,
    fleet_ship_info_index: Option<u32>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl UpdateShipInFleetBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The [`Fleet`] account
    #[inline(always)]
    pub fn fleet(&mut self, fleet: solana_pubkey::Pubkey) -> &mut Self {
        self.fleet = Some(fleet);
        self
    }
    /// The [`FleetShips`] account
    #[inline(always)]
    pub fn fleet_ships(&mut self, fleet_ships: solana_pubkey::Pubkey) -> &mut Self {
        self.fleet_ships = Some(fleet_ships);
        self
    }
    /// The old [`Ship`] Account
    #[inline(always)]
    pub fn old_ship(&mut self, old_ship: solana_pubkey::Pubkey) -> &mut Self {
        self.old_ship = Some(old_ship);
        self
    }
    /// The address indicated as `next` in the `old_ship` account
    #[inline(always)]
    pub fn next(&mut self, next: solana_pubkey::Pubkey) -> &mut Self {
        self.next = Some(next);
        self
    }
    /// The [`Game`] account
    #[inline(always)]
    pub fn game_id(&mut self, game_id: solana_pubkey::Pubkey) -> &mut Self {
        self.game_id = Some(game_id);
        self
    }
    /// The [`GameState`] account
    #[inline(always)]
    pub fn game_state(&mut self, game_state: solana_pubkey::Pubkey) -> &mut Self {
        self.game_state = Some(game_state);
        self
    }
    #[inline(always)]
    pub fn ship_amount(&mut self, ship_amount: u16) -> &mut Self {
        self.ship_amount = Some(ship_amount);
        self
    }
    #[inline(always)]
    pub fn fleet_ship_info_index(&mut self, fleet_ship_info_index: u32) -> &mut Self {
        self.fleet_ship_info_index = Some(fleet_ship_info_index);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = UpdateShipInFleet {
            fleet: self.fleet.expect("fleet is not set"),
            fleet_ships: self.fleet_ships.expect("fleet_ships is not set"),
            old_ship: self.old_ship.expect("old_ship is not set"),
            next: self.next.expect("next is not set"),
            game_id: self.game_id.expect("game_id is not set"),
            game_state: self.game_state.expect("game_state is not set"),
        };
        let args = UpdateShipInFleetInstructionArgs {
            ship_amount: self.ship_amount.clone().expect("ship_amount is not set"),
            fleet_ship_info_index: self
                .fleet_ship_info_index
                .clone()
                .expect("fleet_ship_info_index is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `update_ship_in_fleet` CPI accounts.
pub struct UpdateShipInFleetCpiAccounts<'a, 'b> {
    /// The [`Fleet`] account
    pub fleet: &'b solana_account_info::AccountInfo<'a>,
    /// The [`FleetShips`] account
    pub fleet_ships: &'b solana_account_info::AccountInfo<'a>,
    /// The old [`Ship`] Account
    pub old_ship: &'b solana_account_info::AccountInfo<'a>,
    /// The address indicated as `next` in the `old_ship` account
    pub next: &'b solana_account_info::AccountInfo<'a>,
    /// The [`Game`] account
    pub game_id: &'b solana_account_info::AccountInfo<'a>,
    /// The [`GameState`] account
    pub game_state: &'b solana_account_info::AccountInfo<'a>,
}

/// `update_ship_in_fleet` CPI instruction.
pub struct UpdateShipInFleetCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// The [`Fleet`] account
    pub fleet: &'b solana_account_info::AccountInfo<'a>,
    /// The [`FleetShips`] account
    pub fleet_ships: &'b solana_account_info::AccountInfo<'a>,
    /// The old [`Ship`] Account
    pub old_ship: &'b solana_account_info::AccountInfo<'a>,
    /// The address indicated as `next` in the `old_ship` account
    pub next: &'b solana_account_info::AccountInfo<'a>,
    /// The [`Game`] account
    pub game_id: &'b solana_account_info::AccountInfo<'a>,
    /// The [`GameState`] account
    pub game_state: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: UpdateShipInFleetInstructionArgs,
}

impl<'a, 'b> UpdateShipInFleetCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: UpdateShipInFleetCpiAccounts<'a, 'b>,
        args: UpdateShipInFleetInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            fleet: accounts.fleet,
            fleet_ships: accounts.fleet_ships,
            old_ship: accounts.old_ship,
            next: accounts.next,
            game_id: accounts.game_id,
            game_state: accounts.game_state,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(6 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.fleet.key, false));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.fleet_ships.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.old_ship.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.next.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.game_id.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.game_state.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = borsh::to_vec(&UpdateShipInFleetInstructionData::new()).unwrap();
        let mut args = borsh::to_vec(&self.__args).unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::SAGE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(7 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.fleet.clone());
        account_infos.push(self.fleet_ships.clone());
        account_infos.push(self.old_ship.clone());
        account_infos.push(self.next.clone());
        account_infos.push(self.game_id.clone());
        account_infos.push(self.game_state.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `UpdateShipInFleet` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` fleet
///   1. `[writable]` fleet_ships
///   2. `[]` old_ship
///   3. `[]` next
///   4. `[]` game_id
///   5. `[]` game_state
#[derive(Clone, Debug)]
pub struct UpdateShipInFleetCpiBuilder<'a, 'b> {
    instruction: Box<UpdateShipInFleetCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> UpdateShipInFleetCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(UpdateShipInFleetCpiBuilderInstruction {
            __program: program,
            fleet: None,
            fleet_ships: None,
            old_ship: None,
            next: None,
            game_id: None,
            game_state: None,
            ship_amount: None,
            fleet_ship_info_index: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The [`Fleet`] account
    #[inline(always)]
    pub fn fleet(&mut self, fleet: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.fleet = Some(fleet);
        self
    }
    /// The [`FleetShips`] account
    #[inline(always)]
    pub fn fleet_ships(
        &mut self,
        fleet_ships: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.fleet_ships = Some(fleet_ships);
        self
    }
    /// The old [`Ship`] Account
    #[inline(always)]
    pub fn old_ship(&mut self, old_ship: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.old_ship = Some(old_ship);
        self
    }
    /// The address indicated as `next` in the `old_ship` account
    #[inline(always)]
    pub fn next(&mut self, next: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.next = Some(next);
        self
    }
    /// The [`Game`] account
    #[inline(always)]
    pub fn game_id(&mut self, game_id: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.game_id = Some(game_id);
        self
    }
    /// The [`GameState`] account
    #[inline(always)]
    pub fn game_state(
        &mut self,
        game_state: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.game_state = Some(game_state);
        self
    }
    #[inline(always)]
    pub fn ship_amount(&mut self, ship_amount: u16) -> &mut Self {
        self.instruction.ship_amount = Some(ship_amount);
        self
    }
    #[inline(always)]
    pub fn fleet_ship_info_index(&mut self, fleet_ship_info_index: u32) -> &mut Self {
        self.instruction.fleet_ship_info_index = Some(fleet_ship_info_index);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = UpdateShipInFleetInstructionArgs {
            ship_amount: self
                .instruction
                .ship_amount
                .clone()
                .expect("ship_amount is not set"),
            fleet_ship_info_index: self
                .instruction
                .fleet_ship_info_index
                .clone()
                .expect("fleet_ship_info_index is not set"),
        };
        let instruction = UpdateShipInFleetCpi {
            __program: self.instruction.__program,

            fleet: self.instruction.fleet.expect("fleet is not set"),

            fleet_ships: self
                .instruction
                .fleet_ships
                .expect("fleet_ships is not set"),

            old_ship: self.instruction.old_ship.expect("old_ship is not set"),

            next: self.instruction.next.expect("next is not set"),

            game_id: self.instruction.game_id.expect("game_id is not set"),

            game_state: self.instruction.game_state.expect("game_state is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct UpdateShipInFleetCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    fleet: Option<&'b solana_account_info::AccountInfo<'a>>,
    fleet_ships: Option<&'b solana_account_info::AccountInfo<'a>>,
    old_ship: Option<&'b solana_account_info::AccountInfo<'a>>,
    next: Option<&'b solana_account_info::AccountInfo<'a>>,
    game_id: Option<&'b solana_account_info::AccountInfo<'a>>,
    game_state: Option<&'b solana_account_info::AccountInfo<'a>>,
    ship_amount: Option<u16>,
    fleet_ship_info_index: Option<u32>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
